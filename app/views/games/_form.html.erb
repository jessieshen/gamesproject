 <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :company_id %><br />
	<%= collection_select(:game, :company_id, Company.all, :id, :name)%>
  
  </p>