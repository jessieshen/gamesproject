<h1>Editing game</h1>

<% form_for(@game) do |f| %>

 <%= render :partial => 'form', :locals => {:f => f} %>

<p>
	<%= f.submit 'Update' %>
</p>
<% end %>

<%= link_to 'Show', @game %> |
<%= link_to 'Back', games_path %>